from django.apps import AppConfig


class MiappConfig(AppConfig):
    name = 'miapp'
    verbose_name="Mi Primera Aplicacion" # para que reconosca el cambio se debe cargar la clase en mis app (Settings)
